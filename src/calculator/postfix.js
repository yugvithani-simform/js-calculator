import { Stack } from '../utils/stack.js'

export default function postfix(arr){
    const stack = new Stack()
    while(!(arr.length)){
        // let lastEle = stack.pop()
        // if(lastEle)
    }
}